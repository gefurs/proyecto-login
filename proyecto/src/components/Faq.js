import Navigation from "../routes/Navigation";

const Faq = ({setUser}) => {
    return (
        <>
            <h1>FAQ</h1>
            <Navigation setUser = {setUser} />
        </>
    );
}

export default Faq;